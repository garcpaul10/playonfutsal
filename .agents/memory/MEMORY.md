- [PlayOn DB migrations](playon-db-migrations.md) — always use ensure-schema.mjs (not drizzle-kit push) for schema changes in this non-TTY environment
- [PlayOn drop-in payment flow](playon-dropin-payment.md) — paid pools use POST /dropins/pools/:poolId/checkout → Stripe → webhook creates spot; free pools use /rsvp directly
- [PlayOn free agent notifications](playon-fa-notifications.md) — in-app notifications dispatched at team_reviewing, player_reviewing, and matched transitions in leagues.ts
- [PlayOn admin permission gating](playon-admin-permissions.md) — `useAdminPermissions` hook in src/hooks/ returns scoped can* flags; backend has requireAnyPermission for multi-domain routes
- [ageGroup text[] migration pattern](agegroup-array-migration.md) — leagues/tournaments/camps ageGroup converted to text[] via migration 0037; normalizeAgeGroup helper in every route parser guards both text and text[] DB values
- [Two-tier event model — session shell + pools](two-tier-event-pools.md) — drop-ins fully converted; pool.cap is sole capacity control; leagues/tournaments/camps still pending
- [Courts venueId + split rule hierarchy](courts-venue-split.md) — courts.venue_id added (migration 0054); computeRevenueSplit resolves offering-scoped rules without requiring venueId
- [Guardian child registration](guardian-child-registration.md) — spots.guardian_user_id (migration 0055) tracks parent; ensure-schema verification query must include 'spots' in its IN-clause
- [League divisions layer](league-divisions.md) — league_divisions table (0056–0059); standings/fixtures GET returns {type:"grouped",divisions:[]} when >1 division; flat array when single-division (backward compat)
- [Age group waivers system](age-group-waivers-system.md) — USYS eligibility (July 31 cutoff, two-year bands) enforced in all registration paths; approved waivers bypass check; admin review UI at /admin/age-group-waivers
- [Web push VAPID setup](web-push-vapid.md) — VAPID keys in env vars; web_push_subscriptions table (migration 0071); sw.js in playon/public; push routes on notificationPreferences router
- [Paid waitlist offer flow](paid-waitlist-offer.md) — notify-then-pay: pre-created Stripe session drives expiry; expired player pushed to back of queue before re-dispatch; /spots/:id/pay blocks payment without active offer (403); stripeWebhook.ts uses sendMultiChannelNotification from ../services/notifications (not notificationService)
- [PlayOn onboarding + ID verification](playon-onboarding-id.md) — users.role nullable (migration 0076); profile gate checks roles AND idVerified; codegen run via pnpm --filter @workspace/api-spec run codegen; zod not resolvable in api-server esbuild — define local schemas inline
- [Drop-in template architecture](dropin-template-architecture.md) — template-first lazy materialization; new API at /dropin-templates/* (NOT /dropins/templates which belongs to old session-template system); 5 new tables; wizard at /admin/dropins/new
- [KotC Battle Engine](kotc-battle-engine.md) — 9 tables, custom QR codes, rules-ack gate, grace timer as setTimeout, ensure-schema IN-clause must list KotC tables, composite TS build needed for new db exports
